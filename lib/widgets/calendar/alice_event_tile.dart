import 'package:flutter/material.dart';

class AliceEventTile extends StatelessWidget {
  final Widget child;

  const AliceEventTile({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
