import 'package:flutter/material.dart';

class AliceEventsList extends StatelessWidget {
  final Widget child;

  const AliceEventsList({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
